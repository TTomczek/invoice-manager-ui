import { AuthentificationImplService } from '../service/authentification/authentification-impl.service';

export const environment = {
  production: false,
  authentificationProvider: AuthentificationImplService
};
