import { AuthentificationMockService } from '../service/authentification/authentification-mock.service';

export const environment = {
  production: false,
  authentificationProvider: AuthentificationMockService
};
